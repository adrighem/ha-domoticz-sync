# Standing Rules

- Ask for explicit approval before public GitHub actions such as comments, labels, issue closure, or releases.
- Treat external pull requests as input; implement accepted changes directly.
- Add or update tests when changing parser, API, coordinator, entity, config flow, or diagnostics behavior.
- Use `Refs #NN` footers in commit bodies for issue-related changes.
